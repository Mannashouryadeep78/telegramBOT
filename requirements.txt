# AI Providers
google-genai              # Gemini 2.5 Flash — chat, vision, OCR, JSON
groq                      # Whisper v3 Turbo — audio transcription
openai                    # OpenRouter fallback (OpenAI-compatible client)
fal-client                # fal.ai — video generation (Kling / Wan 2.6)

# Telegram Bot Framework
python-telegram-bot

# Networking & Utilities
httpx
python-dotenv

# Media Processing
opencv-python-headless    # Video frame extraction
Pillow                    # Image handling for Gemini SDK
pypdf                     # (kept for any legacy PDF use)

# Database — sqlite3, json, io, base64 are all built into Python